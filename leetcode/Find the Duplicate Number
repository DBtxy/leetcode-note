不许改变数组，也不许用多余的空间，简单的做法有直接排序，然后扫一遍
稍微变体的做法，在于找环，还需要排除自闭环
找到环之后，找到环的入口，即有两个入度的节点，入口的index+1即为所求

找环算法，用两个指针，一个指针走两步，一个指针走一步，如果相遇就是有环，找到环之后先要
判断是否是自闭环，然后根据算法特性，从头指针和相遇之处的指针开始走，知道相等，相等之后的index+1即为所求


这道题想到这这里就是从0开始探测，那么这时候的复杂度就是不是O(n)了，数据长度是n，那么数字的范围是1~n-1，
考虑坐标从0开始，范围可以映射为0~n-2，所以n-1的位置是没有入度的，从n-1的位置开始绝对不会陷入自闭环
