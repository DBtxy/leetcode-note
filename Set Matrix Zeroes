因为需要在in-place中完成，所以不能再遍历一遍的过程改变值，只能记录特殊的地方然后在下一次遍历中改变
但是想到这里我就卡住了，数组中的书都是任意数，不太好标记，所以这一种标记法行不通
巧妙之处在于可以把矩阵中的零提到左上边界处，那么nums[i][j] = 0 会影响row = i && col = j 两行（列）
为零，提出到边界之后会影响两个加上边界两行为零，那么只需要提前记录边界是否为零即可
